class AppConstant {
  // Here we can define various constants we use in the app
  // We do not use it to store secrets
  static const StoryCacheKey = "apiData";
  static const AccessTokens = 'accessTokens';
  static const RefressTokens = 'refreshTokens';
}

